# تعليمات نشر "وكيلي" خطوة بخطوة

## المرحلة 1: إنشاء حساب GitHub
1. افتح github.com
2. اضغط "Sign up" واملأ البريد الإلكتروني وكلمة مرور
3. أكّد بريدك الإلكتروني

## المرحلة 2: رفع الملفات على GitHub
1. من صفحتك الرئيسية بعد تسجيل الدخول، اضغط "+" أعلى الصفحة ثم "New repository"
2. اسم المستودع: `wakeely-app` (أو أي اسم تحبه)
3. اجعله Public أو Private (لا فرق)
4. اضغط "Create repository"
5. في الصفحة التالية اضغط "uploading an existing file"
6. اسحب هذين الملفين:
   - `index.html`
   - مجلد `api` بالكامل (يحتوي `chat.js`)
7. اضغط "Commit changes"

## المرحلة 3: ربط GitHub بـ Vercel
1. افتح vercel.com
2. اضغط "Sign Up" واختر "Continue with GitHub" (يربطهم ببعض تلقائياً)
3. بعد الدخول، اضغط "Add New" → "Project"
4. اختر المستودع `wakeely-app` من القائمة واضغط "Import"

## المرحلة 4: إضافة مفتاح Claude API بأمان (الخطوة الأهم)
1. قبل الضغط على "Deploy"، افتح قسم "Environment Variables"
2. أضف متغير جديد:
   - **Name:** `ANTHROPIC_API_KEY`
   - **Value:** مفتاحك (تحصل عليه من console.anthropic.com → API Keys → Create Key)
3. اضغط "Add" ثم "Deploy"

## المرحلة 5: التشغيل
1. بعد دقيقة تقريباً، يعطيك Vercel رابط دائم مثل: `wakeely-app.vercel.app`
2. افتح هذا الرابط من Safari أو Chrome على موبايلك
3. اضغط زر المشاركة → "إضافة إلى الشاشة الرئيسية"
4. صار عندك تطبيق حقيقي، بمفتاح API مخفي وآمن، ورابط ثابت دائم

## ملاحظة مهمة
أي تعديل تسويه على الكود لاحقاً وترفعه على GitHub، Vercel يحدّث التطبيق تلقائياً خلال ثوانٍ — بدون ما تعيد كل الخطوات.
